{
  "kind": "oekaki.overlay",
  "version": "0.1",
  "assertions": [
    {
      "assert": "edge.suppress",
      "from": { "node": "aws_ecs_service.api" },
      "to": { "node": "aws_db_instance.orders" }
    }
  ]
}
