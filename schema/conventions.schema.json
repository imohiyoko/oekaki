{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/imohiyoko/oekaki/main/schema/conventions.schema.json",
  "title": "oekaki conventions",
  "description": "Where to look in a Terraform repository for facts the language does not standardise. Values are names, not data: which local holds an account id, which variable holds a table of them.",
  "type": "object",
  "required": ["kind", "version"],
  "additionalProperties": false,
  "properties": {
    "kind": {"const": "oekaki.conventions"},
    "version": {"enum": ["0.1"]},
    "note": {
      "type": "string",
      "description": "Why this repository is read the way it is. For the person who finds the file later."
    },
    "accountFromLocal": {
      "type": "array",
      "description": "Locals that hold an AWS account id outright, most trusted first.",
      "items": {"type": "string", "minLength": 1},
      "uniqueItems": true
    },
    "accountTable": {
      "type": "object",
      "description": "A table of name to account id, and the name a module uses to pick its row.",
      "required": ["variable"],
      "additionalProperties": false,
      "properties": {
        "variable": {
          "type": "string",
          "minLength": 1,
          "description": "The variable holding the table. The same name is looked for as variable.key or variable[\"key\"] to see which row a module takes."
        }
      }
    }
  }
}
